# AGENTS.md — Al Workspace Al 全局规则

## 核心约束（必须遵守）

1. **所有项目/脚本/文档/网页/素材** 一律放在 `D:\Al Workspace Al`，不要散落到桌面、下载、临时目录或 D 盘根目录。
2. 如果该目录不存在，先创建再开始工作。
3. 新项目必须用 `project_NNN_任务关键词` 命名（NNN=三位序列号，自动检测已有项目取下一个）。
4. 每个项目内用标准结构：`00_brief.md` `01_inputs/` `02_work/` `03_outputs/` `04_logs/` `README.md`。
5. 最终交付物只放 `03_outputs/`，不在回复里塞大段代码而是给路径。
6. 任务开始前先写 `00_brief.md`（目标、约束、日期），任务结束前更新状态。
7. 发生分歧、用户不满意、重做、踩坑时，追加到 `01_全局复盘_踩坑日志.md`（格式见该文件）。
8. 交付网页/小游戏时，说明本地文件入口和链接有效期，优先提供可长期保存的本地目录。
9. 系统级改动必须精确到单一组件，先说明风险。
10. 全机扫描类任务先做轻量画像，不要直接长时间全量递归。

## 文件命名规范

- 用 `文件名_v01.md` / `文件名_final.md`，不用 `新建文本文档.txt` / `最终版最终版.md` / `111.docx`。
- 交付文件用有意义的中文或英文名。

## 参考文件

- `01_全局复盘_踩坑日志.md` — 历史踩坑和长期偏好
- `02_工作区_Codex提示词.md` — 完整工作流和命名规则
