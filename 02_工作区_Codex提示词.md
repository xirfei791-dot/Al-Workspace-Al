# 02_工作区_Codex提示词

用途：作为启动新任务时给 Codex 的工作区提示词，统一项目命名、文件放置、上下文记录方式，防止文件散落、上下文混乱，减少重复扫描和算力消耗，提升定位与执行效率。

## 固定工作区

所有项目、脚本、文档、网页、素材整理、临时产物，优先放在：

```text
D:\Al Workspace Al
```

如果该目录不存在，先创建它再开始任务。

## 新任务命名规则

每个新任务都创建独立项目文件夹，使用以下格式：

```text
project_序列号_任务关键词
```

示例：

```text
project_001_开机启动项优化
project_002_视频素材自动归档
project_003_直播字幕工作流
project_004_剪映工程模板
```

命名要求：

- `project` 固定小写英文，方便搜索和排序。
- 序列号使用三位数字：`001`、`002`、`003`。
- 任务关键词使用中文或英文均可，但要短、明确、可搜索。
- 不使用空格，优先使用下划线 `_`。
- 不把多个无关任务塞进同一个项目文件夹。

## 推荐目录结构

每个项目文件夹建议包含：

```text
project_001_任务关键词
├─ 00_brief.md
├─ 01_inputs
├─ 02_work
├─ 03_outputs
├─ 04_logs
└─ README.md
```

目录用途：

- `00_brief.md`：记录任务目标、用户要求、限制条件、当前结论。
- `01_inputs`：放用户提供的原始文件、参考资料、截图、素材。
- `02_work`：放中间脚本、草稿、临时分析文件。
- `03_outputs`：放最终交付文件，只放用户真正要用的结果。
- `04_logs`：放执行记录、排错日志、测试结果。
- `README.md`：记录项目入口、使用方法、关键文件说明。

## 启动新任务时的提示词模板

可以直接复制下面这段给 Codex：

```text
请在 D:\Al Workspace Al 下开始这个任务。
新建项目文件夹，命名规则为 project_三位序列号_任务关键词。
先检查已有 project_* 文件夹，自动选择下一个序列号。
项目内按 00_brief.md、01_inputs、02_work、03_outputs、04_logs、README.md 组织。
所有最终交付物放到 03_outputs。
执行前先把任务目标和约束写入 00_brief.md。
如果任务过程中我要求重改、产生分歧或踩坑，请追加记录到 D:\Al Workspace Al\01_全局复盘_踩坑日志.md。
```

## Codex 执行要求

Codex 在开始项目类任务时，应执行以下步骤：

1. 检查 `D:\Al Workspace Al` 是否存在，不存在则创建。
2. 扫描已有 `project_*` 文件夹，确定下一个三位序列号。
3. 创建新的项目文件夹和标准子目录。
4. 创建 `00_brief.md`，写入任务目标、用户原话摘要、日期、关键约束。
5. 工作文件放 `02_work`，最终文件放 `03_outputs`。
6. 不把交付文件散落到桌面、下载目录、Codex 临时目录或 D 盘根目录。
7. 任务完成后，在最终回复中给出关键文件路径。

## 上下文管理规则

为了减少重复理解和算力消耗，每个项目都要维护最小上下文：

- 当前目标是什么。
- 用户明确说过哪些偏好。
- 哪些方案已经试过，为什么放弃。
- 哪些文件是最终结果。
- 下一步如果继续，应该从哪里开始。

建议在 `00_brief.md` 中维护一个“当前状态”小节。

## 文件命名细则

常见文件建议命名：

```text
brief.md                 任务简述
notes.md                 分析笔记
todo.md                  待办清单
run_log.md               执行日志
error_log.md             错误日志
final_report.md          最终报告
config.json              配置文件
script.ps1               PowerShell 脚本
script.py                Python 脚本
index.html               网页入口
```

如果同类文件有多个版本，使用：

```text
文件名_v01.md
文件名_v02.md
文件名_final.md
```

不要使用：

```text
新建文本文档.txt
最终版最终版.md
111.docx
随便放一下.ps1
```

## 复盘联动

当出现以下情况时，追加到：

```text
D:\Al Workspace Al\01_全局复盘_踩坑日志.md
```

需要记录的情况：

- 用户明确表示不满意。
- 用户要求重做、改方向、撤销之前做法。
- Codex 误解了任务或文件位置。
- 文件放错目录、命名混乱、找不到上下文。
- 工具执行失败且影响任务推进。
- 用户新增了长期偏好或禁忌。

记录格式建议：

```text
### YYYY-MM-DD：简短标题

- 事件：发生了什么。
- 分歧/不满意点：用户不满意或产生分歧的原因。
- 最终决定：这次最后怎么处理。
- 下次做法：以后如何避免。
```

## 快速检查清单

任务结束前检查：

- 是否在 `D:\Al Workspace Al` 下工作。
- 是否使用 `project_三位序列号_任务关键词` 命名。
- 最终文件是否放在 `03_outputs`。
- 是否更新了 `00_brief.md` 的当前状态。
- 如有重改或分歧，是否更新了踩坑日志。
- 最终回复是否给出明确路径。
